spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/library
    username: test
    password: test
  sql:
    init:
      mode: always
      data-locations: # Tests will provide their own data
      schema-locations: classpath:schema.sql
